# =====================================
# 🚀 Database Configuration (PostgreSQL)
# =====================================
DB_HOST=localhost          # Database Host (e.g., localhost, db.example.com)
DB_PORT=5432              # Default PostgreSQL Port
DB_NAME=lowtech_gmbh      # Name of the Database
DB_USER=postgres          # Database Username
DB_PASSWORD=admin         # Database Password (Change this in production!)

# =====================================
# 🌍 Application Configuration
# =====================================
APP_PORT=3000             # Port where the app will run

# =====================================
# 🔑 JWT Authentication
# =====================================
JWT_SECRET=your_jwt_secret_key # Secret Key for JWT (Use a strong random string!)

# =====================================
# ☁️ AWS Configuration
# =====================================
AWS_REGION=your-aws-region              # AWS Region (e.g., us-east-1)
AWS_S3_BUCKET_NAME=your-s3-bucket-name  # AWS S3 Bucket Name
AWS_ACCESS_KEY_ID=your-access-key-id    # AWS IAM User Access Key ID
AWS_SECRET_ACCESS_KEY=your-secret-key   # AWS IAM User Secret Key

# =====================================
# 💰 Stripe Payment Gateway
# =====================================
STRIPE_SECRET_KEY=your-stripe-secret-key  # Stripe API Secret Key (Starts with sk_live_ or sk_test_)

# =====================================
# 📧 AWS SES (Simple Email Service)
# =====================================
AWS_SES_FROM_EMAIL=your-verified-email@example.com  # Must be a verified email in AWS SES
